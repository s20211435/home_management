<%= turbo_stream.replace dom_id(@user), partial: "users/user", locals: { user: @user } %>
<%= turbo_stream.replace "flash", partial: "layouts/flash" %>
<%= turbo_stream.append "users", partial: "users/user", locals: { user: @user } %> <!-- ユーザーリストの更新 -->

